# controller/views/
"""One module per view. Each is a QWidget that takes what it needs from
the app (a `note` callable for the A.L.E.X. console, the process manager
where relevant) and exposes refresh methods the app's timers call."""
